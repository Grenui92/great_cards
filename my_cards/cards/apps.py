from django.apps import AppConfig


class CardsConfig(AppConfig):
    """The CardsConfig class is a configuration class for the cards app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'cards'
