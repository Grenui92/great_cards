from django.apps import AppConfig


class UsersConfig(AppConfig):
    """UsersConfig is a class for user configuration."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'users'
